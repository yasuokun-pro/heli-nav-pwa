# HELI NAV 引き継ぎ資料(Claude Code向け)

最終更新: 2026-07-10 / 現行バージョン: v6.1(PWA・関東AIP空域ポリゴン実装済み)

進行中タスク・バックログは **BACKLOG.md** に分離した(このファイルは恒久情報のみ)。

---

## 1. プロジェクト概要

ヘリコプター運航者(プロの操縦士)個人が業務補助に使うナビゲーションPWA。
Claude.aiのチャット上でv1→v6まで反復開発してきたものを、以後Claude Codeで継続開発する。

**設計方針(必ず維持すること)**
- 依存最小の単一HTML構成(ビルド不要、GitHub Pagesに置くだけで動く)
- 無料・APIキー不要のデータソースを優先
- アビオニクス風ダークUI(配色・フォントは CSS変数で統一)
- 参考情報である旨のディスクレーマーをfooterから消さない
  (正式な航法情報源ではない/AIP・最新チャートで要確認)

## 2. ファイル構成

```
heli-nav-pwa/            … 場所: ~/Claudeディレクトリ/heli-nav-pwa (git管理)
├─ index.html    … アプリ本体(HTML+CSS+JS全部入り)
├─ sw.js         … Service Worker(オフラインキャッシュ)
├─ manifest.json … PWAマニフェスト
├─ icon-192.png / icon-512.png … アプリアイコン(男の子+プロペラ)
├─ icon-512-maskable.png … Android用。⚠ OSが円/角丸に切り抜くので**中央80%に収める**
├─ favicon.ico / icon-y-180.png … タブのアイコン。⚠ 16pxだと顔が潰れるので**「y」マーク**
├─ wordmark-white.png … フッターのクレジット用(暗い背景)
├─ wordmark-brown.png … 印刷面の見出し用(白紙)
├─ README.md     … GitHub Pages公開・インストール手順(ユーザー向け)
├─ BACKLOG.md    … 進行中・未着手タスク(セッション開始時に指示があれば読む)
└─ tools/
   ├─ gen_asp.py       … AIP空域ポリゴン生成(要 shapely)。index.htmlの
   │                      /*ASP_POLY_GEN_START*/〜END区間へ --splice で埋め込む
   └─ asp_poly.gen.js  … 生成物のコピー(参照用)
```

デプロイ: GitHub Pages(main / root)。**2026-07-10公開済み**
- リポジトリ: https://github.com/yasuokun-pro/heli-nav-pwa (public)
- 公開URL: **https://yasuokun-pro.github.io/heli-nav-pwa/** (iPhoneはこれをホーム画面に追加)
- 更新手順: コミット → `git push` するだけ(1〜2分で反映)。
  **index.html等を変更したら次の3つを必ず一緒に上げる**
  (しないと既存端末に更新が届かない / クレジットが古い版を表示したままになる):
  | 場所 | 定数 | 役割 |
  |---|---|---|
  | `sw.js` 先頭 | `VER` | キャッシュ名。**これがサーバ側の版番号** |
  | `index.html` | `VER_TAG` | データファイルのキャッシュバスター兼**動作中の版番号** |
  | `index.html` | `BUILD` | フッターに出す更新日(YYYY-MM-DD) |
  gh CLIで認証済み(アカウント yasuokun-pro)
**ローカルプレビューでも同じ**: Service Workerが古いindex.htmlをキャッシュして
配り続けるため、編集後の動作確認前に必ずVERを上げ、リロードを2回行うこと
(1回目でSW更新、2回目で新キャッシュから表示)。

## 3. 技術スタック

| 用途 | 採用 | 備考 |
|---|---|---|
| 地図 | Leaflet 1.9.4 (cdnjs) | プラグインなし素のLeaflet |
| ベースタイル | 国土地理院 淡色(pale)/ 全国最新写真(seamlessphoto)/ OSM | 3段トグル。無料・キー不要 |
| 雨雲 | RainViewer タイル | §6参照 |
| 他機(ADS-B) | OpenSky Network REST | 失敗時デモデータにフォールバック |
| フォント | IBM Plex Mono(データ表示)/ Noto Sans JP(UI) | Google Fonts。オフライン時はシステムフォントに劣化 |
| 保存 | localStorage | sandboxed iframe等で例外が出る環境向けにtry/catchラッパー(`store`)経由 |

Googleマップは**不採用**(要APIキー・従量課金。2025/3改定で$200クレジット廃止、
Maps JS APIは月1万ロードまで無料)。ユーザーは当面地理院タイルで了承済み。

## 4. 機能一覧と実装箇所(index.html内の目印コメント)

| 機能 | 実装 | 主な関数/変数 |
|---|---|---|
| ポイント登録 | 地図長押し→マゼンタ経路線。**移動は0.4秒保持して解錠(オレンジ)してからドラッグ** | `addWp()` `bindWpPress()` `WP_HOLD` |
| 航法計算 | 区間ごとMC/MH(WCA)/GS/ETE/燃料+合計行 | `trueBrg()` `magOf()` `windTri()` |
| 風三角 | TAS+風向(°T)+風速→WCA・TH・GS | `windTri()` §5参照 |
| 燃料計算 | FF/FOB→必要燃料・着陸時残(30分切りで赤)・Endurance | `redraw()`内 |
| Direct-To | WP/飛行場へ第2コース(シアン破線)。元ルート保持 | `setDct()` `updateDct()` `dctTarget` |
| GPS | watchPositionで自機位置+実測GS(kt) | `gpsWatch` `ownPos` `ownSpdKt` |
| 実GS連動 | LIVEトグルON+速度取得時、ETE/燃料を実測GSで再計算 | `liveGsActive()` `effGs()` |
| 飛行ログ | 離陸/着陸ボタン→OFF/ON/飛行時間/経路を記録、CSV出力 | `logs[]` `saveLogs()` |
| 共有 | 共有コード(HNAV1.base64)/JSON/GPX書出・読込 | `routeObj()` `loadRouteObj()` |
| 空域 | 関東=AIP正式ポリゴン(CTR青/情報圏アンバー/PCA赤、ツールチップに上下限・周波数)。他地域=概略円のまま | `ASP[]` `ASP_POLY[]` `aspLayer` |
| 飛行場情報 | ✈トグル→ICAOマーカー→ポップアップ(種別/標高/滑走路/座標)+D→/ルート追加 | `apLayer` |
| ログの周波数 | ポイントが飛行場ならその周波数、そうでなければ**その地点を含む空域**の連絡先を IDENT/FREQ に自動記入(空域由来はアンバー・ツールチップに空域名)。印刷のNOTESにも通過空域と連絡先を出す | `adFreqOf()` `airspaceAt()` `inArea()` |
| 進入管制区/TCA | 進トグル→AIP AD 2.17添付図から起こしたClass E空域。**図から起こせた分のみ** | `aca.json` `loadAca()` |
| 制限空域 | 制トグル→AIP ENR 5.1の制限空域・警戒区域(52区域)。RJRは赤・R/Wは橙破線 | `res.json` `loadRes()` |
| 縮尺ボタン | NXT PT=自機+次ポイント / ALL=ルート全体。**どちらも追尾を解除する**(解除しないと次のGPS更新で自機中心に引き戻される) | `btnZmNext` `btnZmAll` `setFollow()` |
| 諸元オーバーレイ | 段ごとに表示/非表示を選べる(ハンドルの 時刻/NEXT/ROUTE チップ)。▴▾は選択を保持したまま全部たたむ | `hud` `applyHud()` `setHudOff()` |
| バージョン表示 | フッターのクレジットに動作中の版と更新日。「更新を確認」でサーバのsw.jsと突合 | `VER_TAG` `BUILD` `checkUpdate()` |
| 地点の取り込み | 🔍検索窓に座標/GoogleマップURLを貼るとピン。`?pt=lat,lng&n=名前` や共有ターゲットの `?text=&url=` でも立つ | `parseLatLng()` `pinFromParams()` |
| 元に戻す(↩) | ルートを変える操作の**直前**に `pushUndo()`。40件まで。同じ状態は積まない | `undoStack` `pushUndo()` `doUndo()` |
| 永続化 | ルート・設定・ログ・ベース地図選択を自動保存/起動時復元 | `store` `saveState()` `init()` |

### UI上の注意
- **iOSはボタンのダブルタップで画面が拡大する**。viewportに
  `maximum-scale=1.0, user-scalable=no` を入れ、UI側に `touch-action:manipulation` を当てて止めてある。
  ⚠ 地図(`.leaflet-container`)はLeafletが自前で `touch-action:none` を持つので影響を受けない。
    地図のピンチズーム・ダブルタップズームはLeafletのJSが処理していて生きている
- **ポイントの移動は「保持して解錠→ドラッグ」**(`WP_HOLD`=420ms)。
  触っただけで動く・掴もうとして新しいポイントができる、という誤操作対策。
  ⚠ `draggable:false` にして解錠時に有効化する手は使えない。Leafletは
    touchstart時点で拾っていないと**同じ指の動きでは掴めない**ため、
    常にdraggableのままにして「未解錠なら drag で元の座標に引き戻す」で止めている
  ⚠ **マーカー上の長押しで地図側に新しいポイントができてしまう**。
    Leafletの長押し(contextmenu)は地図に対して出るので、`wpPressing` フラグと
    `target.closest('.wp-pin')` の二重で地図側の処理を飛ばしている
  ⚠ **`setIcon` すると要素が作り直されてリスナーが消える**。
    アイコンを差し替えたら必ず `bindWpPress()` を呼び直すこと(`syncWpIcons`)
- ⚠ **ルートを変える操作には必ず `pushUndo()` を入れること**。
  入れ忘れると「戻るボタンが効かない」という形で出る。実際に
  マーカーのドラッグ・行からの削除・改名・各ポップアップの「ルート追加」で
  抜けていた(7箇所)。`addWp(` `wps.splice` `w.name=` を触るときは必ず確認する

## 5. 計算仕様(変更時は要注意)

- 距離: haversine、地球半径 `R_NM=3440.065`(NM)
- 真方位: 大圏初期方位 `trueBrg()`
- **磁方位 = 真方位 + 西偏差**(`varW`)。
  日本は全域西偏(概ね5〜11°W)なので加算方向で正しい。東偏対応は未実装(負値入力で代用可)
- **偏差の自動推定(AUTO)**: AIP各AD 2.2のMAG VAR実測値(105空港)を
  緯度経度の2次式で最小二乗近似(`MAGV_C`/`magVarAt()`、最大誤差1.1°・RMS0.39°)。
  基準点は 自機位置 → ルート中央 → 地図中心 の順(`autoVarRef()`)で、
  redrawとGPS更新のたびに追従する。偏差欄を手入力するとAUTOが外れ(`varAuto`)、
  AUTOボタンで復帰。**AIP値自体1°丸めなので、厳密な値はAIP/チャートで確認**。
  係数の再フィットは `tools/gen_magvar.py`(AIRAC更新で偏差がずれてきたら実行)
- 風三角(`windTri(tc)`、風向は「吹いてくる」**真方位**):
  - `rel = wd − tc`、横風 `xw = ws·sin(rel)`(+は右から)、向い風 `hw = ws·cos(rel)`
  - `WCA = asin(xw/TAS)`(風上へ修正、右風なら+)
  - `TH = TC + WCA`、`GS = TAS·cos(WCA) − hw`
  - `|xw|≥TAS` または `GS≤0` は `{bad:true}` → UIは「不可」表示
- 実測GS: `p.coords.speed`(m/s)×1.9438。null(静止・非対応端末)なら計画値へ自動フォールバック。有効条件は `>2kt`
- ETE表示 `fmtHM`(h:mm)、方位表示 `fmt3`(3桁、000は360)
- 燃料残警告: 着陸時予測残 < FF×0.5(=30分)で赤

## 6. 外部API・データソースの注意点

### Googleマップからの地点取り込み(2026-08 実装)
- **iOSの共有メニューにこのアプリは出せない**。共有先になる仕組み(Web Share Target)は
  **Safari/iOSが非対応**。manifestに `share_target` は入れてあるので
  Android・デスクトップChromeでは共有先に出る
- iPhoneでの現実的な手順は2つ:
  1. Googleマップで地点を長押し → 表示された緯度経度をコピー → 🔍に貼る
  2. ショートカットアプリで「共有シートに表示」ONのショートカットを作り、
     `?pt=緯度,経度` でアプリを開く(共有メニューにはショートカット名で出る)
- `parseLatLng()` が拾える形(優先順):
  | 形 | 例 | 備考 |
  |---|---|---|
  | `!3d..!4d..` | data=…!3d35.68!4d139.76 | **地点そのもの。最優先** |
  | `q=` `ll=` `daddr=` | ?q=loc:35.6,139.7 | 明示指定 |
  | 度分秒 | 35°39'31"N 139°44'43"E | 「座標をコピー」の形 |
  | `@lat,lng` | /maps/@35.6,139.7,17z | **地図中心**で地点とは限らないので最後 |
  | 素の数値 | 35.6586, 139.7454 | 前後が数字でないことを要求 |
- **Plus Code(Open Location Code)に対応**。⚠ **Googleマップは地点によって
  緯度経度を出さない**(施設に吸着すると名称と住所だけ)が、Plus Codeは必ず出る。
  `decodeOLC()` は google/open-location-code の `test_data/decoding.csv` **420件で検証済み**、
  短縮形の復元 `recoverOLC()` は `shortCodeTests.csv` **19件で検証済み**
  (テストページ: `tools/test_olc.html` / `tools/test_olc_short.html`)。
  ⚠ 短縮形(`MPX2+7C`)は**桁を繋ぐだけでは駄目**で、基準点に一番近い升目を選ぶ
    調整が要る。省くと18件中13件が隣の升目(数km〜数十kmずれ)になった
  ⚠ 短縮形は**基準点しだいで全く別の場所になる**。Googleマップが併記する地名
    (「MPX2+7C 東京都港区」)をジオコーダで引いて基準にしている。
    地名が無い場合は地図の中心を使うので、遠くを映していると誤る
- ⚠ **全角が混ざると正規表現が全部外れる**。iOSのコピーで実際に混ざるので、
  数字・カンマ・引用符をまとめて半角に正規化してから解析する
- ⚠ **共有で出る `maps.app.goo.gl` の短縮URLには座標が入っていない**。
  リダイレクトを追う必要があるがCORSでブラウザからは追えない。
  検出したら「座標をコピーして貼って」と案内する(黙って失敗させない)


- **RainViewer**: 正攻法は `api.rainviewer.com/public/weather-maps.json` から最新タイムスタンプ取得。
  fetchがCSP等で失敗する環境向けに `floor(now/600)*600−600` で推定するフォールバックあり(`rainTs()`)。
  国内精度重視なら気象庁ナウキャストタイルへの置換を検討(basetime取得のJSONが必要)
- **OpenSky**: 匿名利用はレート制限が厳しい(数十秒間隔)。地図のbboxでクエリ。
  失敗・0件時はデモ4機を表示し「DEMO」タグ明示。**ADS-B/モードS搭載機しか映らない**旨をUIに残すこと。
  本格運用ならOpenSkyアカウント認証 or ADS-B Exchange(有料)検討
- **地理院タイル**: 出典表記(attribution)必須。淡色 `xyz/pale/{z}/{x}/{y}.png`、
  写真 `xyz/seamlessphoto/{z}/{x}/{y}.jpg`
- Claude.aiのアーティファクトプレビュー内では外部fetch/geolocation/localStorage/SWが
  制限されることがある(全てtry/catchでフォールバック済み)。**本番評価はGitHub Pages上で行う**

## 7. データモデル

```js
// waypoint(メモリ上)
{name:'WP1', lat:35.5, lng:139.7, marker:<Leaflet Marker>, isWp:true}

// 共有コード = 'HNAV1.' + base64(JSON) … 今のルート1本
{v:1,
 set:{tas,wDir,wSpd,varW,ff,fob},
 wp:[[lat,lng,'名称'], ...]}

// まとめ共有 = 'HNAVL1.' + base64(JSON) … 保存一覧から複数選択したもの
{v:1, t:'lib',
 routes:[{name,folder,color,o:<上のルート>}, ...],
 pts:[{name,folder,color,icon,lat,lng}, ...]}
// 取り込みは libImport()。同名は上書きか「(2)」を付けるかを選ばせる

// 端末データ書出 … localStorageのキーをまとめたもの(引っ越し・バックアップ用)
{app:'HELI NAV', ver, at, data:{'hnav.route':…,'hnav.routes':…,'hnav.pts':…,
 'hnav.logs':…,'hnav.draws':…,'hnav.base':…,'hnav.varAuto':…,'hnav.hud':…}}

// 飛行ログ(logs[] / localStorage 'hnav.logs')
{date:'2026/7/8', off:'09:12', on:'10:03', dur:'00:51', route:'WP1→WP2'}

// 空域・飛行場マスタ ASP[](概略円+飛行場マーカー兼用)
{n:'東京(羽田)', icao:'RJTT', t:'ctr'|'inf'|'ap', lat, lng,
 r?:半径km(既定9), elev?:ft, rwy?:'文字列',
 poly?:1}  // poly=1: ASP_POLYに正式形状あり→概略円を描かない。t:'ap'=圏なし飛行場

// AIP正式空域 ASP_POLY[](tools/gen_asp.py が自動生成・手編集禁止)
{n:'東京PCA NR1', icao:'RJTT', t:'ctr'|'inf'|'pca',
 up:6000, lo:1500,   // 上限/下限 ft (lo=0はSFC)
 rmk:'連絡先周波数・運用時間等', pts:[[lat,lng],...]}

// localStorageキー
'hnav.route' / 'hnav.logs' / 'hnav.base'(ベース地図index)
'hnav.hud' … 諸元オーバーレイの段の表示状態 {t,n,d,off}
             t=時刻+FLIGHT TIME+T/O・L/D / n=NEXT / d=ROUTE・TAS・偏差
             off=1 は「選択は残したまま全部たたむ」
```

## 8. AIP空域データの取り扱い(2026-07確立・重要)

**関東の空域ポリゴン化は完了**(AIRAC 2026-07-09基準)。以下は恒久ノウハウ。

### データソース(旧AIS Japanは廃止済み)
- **AIS Japanは2025年に廃止** → 現在は **SWIMポータル**(https://top.swim.mlit.go.jp/swim/、
  要ログイン)からAIP一式をダウンロードする。SWIMは要ログイン・要契約なので
  アシスタントからは取れない。ユーザーがDLして
  `~/Downloads/1_AIP (PDF)/<日付>/` に置く運用
- **.go.jp が一律に見られないわけではない**。多くの官公庁サイトはアプリ内ブラウザ
  (Claude Browser)で普通に開ける。ただし **curl / Python の urllib は
  Cloudflareのbot判定で403** になるサイトがある(mod.go.jp など)。
  その場合はブラウザで開いて `document.body.innerText` から拾い、
  結果を `tools/*.json` に固定してスクリプトから読む(→ `tools/mod_facilities.json`)
- **管制圏・情報圏の水平/垂直限界は ENR 2.1 ではなく AD2(各飛行場)の AD 2.17** に載っている
  (当初想定のENR 2.1はFIR/ACCセクターのみ)。特別管制区(PCA)は
  RJTT/RJAAのAD2内チャート(座標表つき)。民間訓練試験空域は ENR 5.3.1
- **調布(RJTF)の情報圏は廃止済み**(AD 2.17=Nil)。東京ヘリポート(RJTI)はAD2自体なし
- PDF処理: `pdftotext -layout`(brew poppler)。チャート図の区分読解は
  `pdftoppm -png -r 300` で画像化 → 座標プロットとの重ね合わせで検証する手法が確立済み

### AIRAC更新手順(28日ごと・半自動)
1. ユーザーが新AIRACのAIP一式をSWIMからDL
2. 関東各飛行場のAD 2.17とPCAチャートの差分を確認
3. 変更があれば `tools/gen_asp.py` のSPECを修正 →
   `python3 tools/gen_asp.py --splice` で index.html を更新
4. `sw.js` の VER をインクリメントしてコミット

### 全国データ(2026-07追加)
- **飛行場マスタ**: `tools/gen_ad.py` → `ad.json`(**134件**)。AIP各飛行場の
  **AD 2.2/AD 3.2**(ARP・標高)と **AD 2.12/AD 3.12**(滑走路)から起こす。
  レイヤーON時に読み込んで `ASP[]` に合流させる(`loadAd()`→`buildApLayer()`)。
  - **これまで飛行場レイヤーは「管制圏・情報圏を持つ飛行場」だけ66件だった**。
    圏の無い飛行場(佐渡・但馬・奥尻・大館能代・粟国・慶良間・小松島など)が
    丸ごと欠けていた。既存分も標高・滑走路をAIP値で上書きする
  - ARPの書式は揺れる: 区切りが空白/「/」、普天間は粗い値が先に来る、
    鳥取は座標が見出しの**1行上**、ヘリポートは「Heliport reference point」。
    見出しの前2行〜後400字を見て、秒まである長い方を採る
  - 和名はAD2に無いので **GEN 2.4(位置指示記号)の対照表**から取る
  - **運用時間**は AD 2.3/AD 3.3 の **ATS 行**(無ければ AD Administration)。130/134件。
    UTC表記。「H24」「2330-0800 / Other time 1HR PN」等そのまま出す
  - **場周経路は本文にある飛行場が少ない**(11件)。大半は **AD 2.24のチャートにしか無い**
    ので、本文が取れないものは「チャート参照」と出す。抜粋は原文の一部であって
    要約ではない旨をUIに残すこと
  - **回転翼機の方式は調布RJTFだけ**が `(ROTOR CRAFT)/（回転翼機）` の見出しで
    公示している。「4. Parking area for helicopters」のような節見出しを拾わないよう、
    括弧付きの見出しに限定すること
  - **AIPにAD2/AD3のページが無い飛行場**(滝ヶ原RJATなど。GEN 2.4にのみ載る)は
    `EXTRA` に座標直書き。`src` を持たせてポップアップの出典表示も変える
- **nav aids**: 全国VOR/DME/TACAN/NDB 112施設。ENR 4.1から `tools/gen_navaids.py` で抽出、
  index.htmlの `/*NAVAIDS_GEN_*/` へ埋込。◈NAVボタンで表示、タップで情報
- **全国空域**: 非関東は各AD2の AD 2.17 半径から概略円(外接円近似)。
  `tools/natl_ctr.json`(全AD2抽出済) → `gen_asp.py gen_natl()` で ASP_POLY に追加。
  大量ポリゴンは **Canvasレンダラ(L.canvas)** で描画(SVGだと重い)
- **METAR**: aviationweather.govはブラウザCORS不可 → **GitHub Actions**
  (`.github/workflows/metar.yml`, 毎時05/35分)が取得して `metar.json` をコミット。
  PWAは同一オリジンで取得。局リスト=`tools/metar_stations.json`(66局)。
  ※ワークフロー編集には gh の `workflow` スコープが必要(認証済)
  - **自衛隊単独の飛行場はMETARが国際配信されない**(NOAAに元データが無い):
    立川RJTC・入間RJTJ・厚木RJTA・木更津RJTK・館山RJTE・宇都宮RJTU・下総RJTL・
    小牧RJNA・浜松RJNH。運用時間のみ観測の小空港(天草RJDA・屋久島RJFC・久米島ROKJ)も同様。
    米軍横田RJTYと共用飛行場(百里RJAH=茨城 等)は配信あり。
    正確には「元データが無い」のではなく**国際交換(OPMET)に載らない**だけで、
    気象庁の**国内配信**には存在する(電文はRMKが国内様式で末尾が `=`)
    → **近傍局の値を代わりに出すのはやめた**(誤解のもと。ユーザー判断)。
    配信の無い飛行場は**外部サイトへのリンク**を出す。地域番号の対応表は
    `tools/gen_imoc.py` → `/*IMOC_GEN_*/`(123空港)
    - **IMOC 地域一覧** `…&Area=<n>&List=1` … その地域の全局が**1ページ**に出る。
      1局ずつ開かずに済むのでMETARタブの見出しにも常設(`nearImocArea()`で地域判定)
    - **IMOC 単局** `…&Area=<n>&Port=<ICAO>` … 12時間ぶん
    - **sunny-spot.net** `metartaf/index2.html?location_id=<ICAO>` …
      1局ずつだが**約4日ぶんの履歴**が1ページ。areaパラメータは無視される。
      立川・入間とも最新まで載っていることを確認済み(2026-07-30)
  - **SWIMの気象情報は個人では契約できない**(ユーザー確認済み)。
    気象業務支援センターの配信は 開設5万円+基本4,200円/月+航空データ18,000円/月。
    IMOCのスクレイピングはライセンス外の再配信になるので**やらない**。
    リンクで誘導するだけなら規約上の問題がない
  - GitHub Actionsのcronは**ベストエフォート**で数時間遅延・スキップされる。
    METARが古いことがある(タブに更新時刻UTCを表示済み)

- **自衛隊・在日米軍施設**: AIPに載るのは飛行場だけなので、駐屯地・分屯地・演習場は
  外部から拾う。`tools/gen_jsdf.py` → `jsdf.json`(**344件**・111KB)。
  種別 飛/駐/演/基、所属 陸/海/空/米/防。演習場は赤▲、他は所属の1文字で色分け。
  - **2ソースの混在**: OSM(Overpass)=敷地の**外形ポリゴン**273件 /
    ウィキペディア日本語版のカテゴリ+座標=OSMに無い**点データ**47件(`w:1`・ピンの枠が破線)。
    ウィキペ側は座標だけなので外形は出ない
  - **外形の有無にかかわらずピンを必ず立てる**(ポリゴンだけだと一目で気づけないため)。
    名称ラベルはズーム11以上でのみ表示(`jsRelabel()`)
  - **出典表示が必須**(ODbL/CC BY-SA)。レイヤーON時に地図クレジットへ自動追加し、
    ポップアップにも件ごとの出典を出す。**消さないこと**
  - 位置・範囲は保証されないので参考表示に留めること
  - Overpassは全国1クエリだとタイムアウトするので**4分割**+8秒スリープ。
    国外(舟山・千島)がbboxに入るので除外箱を持っている。
    **緯度経度の大小で切ると先島諸島を巻き込む**ので必ず箱で書くこと
  - ja.wikipedia APIは連打すると**429**。1秒スリープ+リトライ必須
  - **ウィキペディアのカテゴリには廃止済みの施設も入る**(檜町駐屯地・芝浦分屯地は
    2000年に廃止)。記事冒頭**2文だけ**を見て過去形の存在表現で落とす(`WP_GONE`)。
    ⚠「返還」「移転し」を入れると現役の恩納分屯基地・千歳基地・熊谷基地まで落ちる
  - **ウィキペディアの記事に座標が無いものが多い**(大宮・習志野・座間・板妻・
    高遊原など)。冒頭の住所を **国土地理院のジオコーダ**
    (msearch.gsi.go.jp/address-search/AddressSearch)にかけて座標化する(`g:1`)。
    ⚠ **番地・丁目まで無い住所は使わない**(「長崎県佐世保市の複数の地区」等は
      市町村の中心に落ちて誤解を招く。16件を見送っている)
  - カテゴリは `陸上自衛隊駐屯地`(178) `航空自衛隊の基地`(72) も見る。
    `自衛隊基地` だけだと駐屯地が大量に漏れる
  - OSMにも無く記事も無い施設は `EXTRA_PT` に座標直書き
  - 軍事タグが付かないが載せたい施設は `EXTRA` にOSMのID直指定で書く
    (首相官邸=relation/7826113、内閣府8号館=way/145495603)。取得は
    **OSM公式API**から(Overpassは混むと落ちる)。所属コードは **'官'**=政府。
    所属コードを増やしたら index.html の `JS_COL` / `JS_SVC` も更新すること
  - OSMの名称が通称と違うものは `NAME_FIX` で直す。市ヶ谷は
    **「防衛省市ヶ谷地区」**で登録されていて「駐屯地」も「基地」も付かないため
    名前での絞り込みから漏れていた(→市ヶ谷駐屯地・所属は防衛省)
  - **同種の漏れが沖縄の在日米軍施設に多数ある**(キャンプ・コートニー等の
    中黒付きカタカナ、出砂島/鳥島/沖大東島の射爆撃場、トリイ通信施設、
    牧港補給地区など約40件)。KEEPの語彙を増やせば拾えるが、
    Barracks 3435やH60(掩体)のような基地内の細かい物件も混ざるので未対応
  - **3自衛隊の公式一覧と機械的に突合している**(`mod_check()`)。
    `tools/mod_facilities.json` は名称+番地までの住所を持つ突合表:
    | 所属 | 件数 | 出典 | 扱い |
    |---|---|---|---|
    | 陸 | 163 | `/gsdf/station/{na,nea,ea,ma,wa}/` | `m:'add'` 無ければ追加 |
    | 空 | 72 | `/asdf/base/{north,center,centerkanto,west,south}/` | `m:'add'` |
    | 海 | 28 | `/msdf/about/org/` | **`m:'chk'` 報告だけ** |
    ⚠ mod.go.jp は **Cloudflareで curl/urllib は403**。更新はブラウザで開いて
      拾う(陸は `document.body.innerText`、空・海は `table` のセル)
    ⚠ **海自の一覧は「第2航空群司令部」のように部隊名**で施設名ではない。
      そのままピンにすると分かりにくいので `chk`(近傍3kmに何も無ければ★を出す)。
      江田島がこれで見つかったので `EXTRA_PT` に施設名を付けて足した
    ⚠ 公式サイト側の誤り・落とし穴(`mod_facilities.json` で補正済み):
      硫黄島分屯基地の住所が**入間基地内の事務所**/長沼分屯基地が「タ張郡」と誤記/
      稚内分屯地の住所行に市名が出ない
    突合は2段構え:
      1. **同名があれば距離を見ずに同一**とする。礼文・別海・日高は敷地が広く
         住所が「字◯◯」だけなので、ジオコーダの点が外形中心から10km近く離れる
      2. 同名が無ければ「名前の芯が一致」かつ6km以内。ただし
         **演習場・射撃場は駐屯地とは別の場所**なので同一扱いにしない
         (`MOD_SAME`)。習志野駐屯地は習志野演習場の2km南にある
    ⚠ **名前の比較は必ず `nz()` を通すこと**。「ヶ」と「ケ」の揺れで
      経ケ岬分屯基地/経ヶ岬分屯基地、霞ケ浦分屯基地/霞ヶ浦駐屯地 が
      別物と判定されて二重登録される
    これで陸9件(静内/相馬原/習志野/出雲/北徳島/富山/瀬戸内/崎辺/南那覇)+
    空1件(高良台)が出た。**空自は72件中71件が既に入っていた**
    さらに**外形の無い点データが公式住所と10km以上ずれていたら公式側を採る**
    (玖珠駐屯地と湯布院駐屯地はウィキペディア由来の座標が入れ替わっていた)
  - 最後に**同名で「外形あり」と「点だけ」が残ったら点の方を消す**
    (秋田・釧路・白老・えびの・当別・山田)。OSMの外形とウィキペの座標が
    数km離れていて前段のマージをすり抜けたもの。
    ⚠ **布引山演習場のように同名で本当に2区画ある**ものは消さないこと
      (両方に外形がある場合は残す)

- **進入管制区・TCA(AD 2.17 添付チャート)**: `tools/gen_aca.py` → `aca.json`。
  - **AD 2.17の表には座標が無い**。全空港が「SEE ATTACHED CHART」。
    ただし図の多くは **(1)(2)… の番号付き座標表**を持ち、テキスト層から機械抽出できる。
    抽出結果は `tools/aca_points.json` に保存(13チャート311点)。
    | チャート | 点数 | | チャート | 点数 |
    |---|---|---|---|---|
    | RJTT/ACA 東京 | 59 | | RJAH/TCA 百里 | 30 |
    | RJFF/ACA 福岡 | 55 | | RJTY/ACA 横田 | 22 |
    | RJAH/ACA 百里 | 42 | | RJCC・RJCJ/ACA 千歳 | 21 |
    | RJFZ/ACA 築城 | 36 | | ROAH/ACA 那覇 | 13 |
    RJBB(関西55点)・RJGG(中部)・RJCO は段組が違って1〜2点しか取れていない(要対応)
  - **区画の繋ぎ方は図を見ないと分からない**ので、座標は自動・構成は手書き
    (gen_tra.py と同じ作り)。SPEC に外形と副区画のリングを書く
  - ⚠ **検算を必ず通すこと**。副区画の**球面**面積の合計が外形と一致しなければ
    読み違えている。横田ACAでは (17)(18)(19) の三角形を取りこぼして0.3%ずれ、
    8000区画が (19)→(17)→(18) と回ると分かって 0.0000% になった。
    平面近似だと緯度幅の広い外形で0.3%の誤差が出るので**球面公式で検算する**
  - ⚠ 高度ラベルの読み方: 「上の線 / 上限 / 下限 / 下の線」。
    東京ACAの「FL240 / 4000 (EXC 4000)」は上限FL240・下限4000(4000を含まない)。
    **数値1つのラベルが上限か下限かは図ごとに違う**(下の⚠⚠を必ず読むこと)。
    横線は2段ラベルでも単独ラベルでも数値の上に引かれるので、**手掛かりにならない**。
    無い側を勝手にGND/UNLと決めないこと(null で出して「図に記載なし」と表示)
  - **区画の読み取りには `gen_aca.py --annot <ICAO> <page>` を使う**。
    図の (1)(2)… ラベルは `pdftotext -bbox-layout` でページ座標が取れるので、
    座標表と突き合わせてアフィン変換を作り、**真の点位置を打ち直した画像**を出す。
    ⚠ ラベルは点から少しずれて置かれる。ラベル位置のまま読むと、
      東京ACAの(21)〜(30)のような密集部で必ず取り違える
  - 検算は shapely でも行える(区画同士の重なりを面積で検出できる)。
    合計面積の一致だけだと「どこが違うか」が分からないので併用する
  - リングの要素は点番号のほか**円弧も書ける**(`{'arc':(lat,lon),'r':NM,'from':..,'to':..}`、
    線分と円の交点は `{'cut':..,'seg':(a,b)}`)。東京ACAの3000(霞ヶ浦ARPから5NM)と
    4000北西(362923N/1395148Eから15NM)がこれを使う
  - 弧を含む区画があると面積の完全一致は望めないので、`remainder` を書いた空域は
    **shapelyで区画同士の重なりと外形はみ出しを見る**方式に切り替える。
    残り(下限の記載が無い区域)は外形から引いて作るので閉じることは保証される
  - ⚠ **同心円弧で組まれた図(百里TCA型)は公表座標だけでは起こせないことがある**。
    百里TCAは弧の中心をARP/VOR/当てはめのどれにしても公称半径と0.5NM合わず、
    区画の角(弧と放射線の交点)が座標表に無い。着手前に
    「表の点が公称半径に乗るか」を確かめること(乗らなければ深追いしない)
  - ⚠ `--annot` は `/tmp/aca_pg-*.png` を毎回消してから描く。
    消さないと前回の別ページの画像を拾う(百里で page 9 を指定して page 10 が出た)
  - ⚠ **1つのラベルが引き出し線2本で別々の2区画を指すことがある**
    (東京ACAの 8000(EXC 8000))。1ラベル=1区画と思い込むと形を間違える
  - ⚠⚠ **隣り合う進入管制区は同じ区画を共有していて、複数の図に同じものが載る**。
    百里ACAは42点中**33点が東京ACAと同一座標**、横田ACAは4区画が**重なり100%**だった。
    そのまま出すと二重に描かれるので、生成器が最後に突合して
    **上下限の揃っている図を優先し、後の図はその差分だけ残す**。
    ⚠ 単純に捨てると、百里の7000のように東京より北へ広い版が丸ごと消える
      (東京の図はそこで切れているだけで、空域は続いている)
  - ⚠⚠ **図によって数値の意味が違う**。
    東京は「FL240 / 4000 (EXC 4000)」= 上限/下限の2段。
    **横田・百里は数値が1つで、それは下限**。
    根拠: 同一座標の4区画を突合したところ、東京の図では同じ数値が下段(下限)だった。
    上限しか無いと思って実装して間違えたので、**必ず確かめてから書くこと**。
    見分け方は次の2つ。隣の図が無い千歳はこれで決めた:
    1. **同じ図の中の2段ラベルと突き合わせる**。千歳は単独が「FL200」「FL160」で、
       2段が「FL200/11000」「FL160/8000」と**上段に同じ値**が来ている。
       1枚の図の中で FL200 が上限にも下限にもなる読み方は成り立たないので、
       単独値=上限と決まる(東京と同じ型)
    2. **その飛行場自身がどの区画に入るか**を見る。千歳(RJCC/RJCJ)は
       単独ラベル FL200 の区画の中にある。そこが下限FL200では進入管制にならない
  - ⚠ **番号付き座標表を持たない図がある**(宇都宮・浜松は座標が図中に直接書かれている)。
    自動抽出に掛からないので `EXTRA_POINTS` に手入力する
  - ⚠ **「宇都宮RDR」「浜松RDR」はレーダーの探知範囲ではない**。AD 2.18の
    APP/ASR の呼出符号(Utsunomiya Radar / Hamamatsu Radar)で、
    **その覆域＝進入管制区(ACA)**。このレイヤーがそのまま答えになる
  - ⚠ **図の注記が実際の境界と食い違うことがある**。浜松ACAの
    「30NM fm 351534N/1365459E」はどの外周弧とも一致せず、実際は LHT 20NM だった。
    **注記を信じる前に、候補の弧をチャート画像に重ねて確かめること**
    (`--annot` と同じ要領。座標に番号が無い図は座標ラベルの位置から変換を作る)
  - ⚠ 弧を**別々の区間に分けて使う**ときは、交点を**両方の区画で頂点にする**こと。
    片方だけ通しの弧にすると角が切れて隙間ができる(浜松で1.3km²)
  - ⚠ **同じ「45NM」の注記が別々の中心の弧を指していることがある**。
    千歳は南東の45NMが座標表の(6)中心、南西の45NMが**函館VOR(HWE)中心**だった。
    中心を1つと思い込むと(15)が(6)から58.6NMになって合わない。
    **弧上に乗るはずの点までの実測距離を全部出して、揃う中心を探すこと**
  - 実装済みは **東京ACA(20区画)・横田ACA・百里ACA・宇都宮ACA・浜松ACA・千歳ACA**
    (重複除去後で計32区画)。UIに「図から起こせた空域のみ」と明記している
    - 千歳ACA(6区画・0.0000%一致)。**新千歳RJCCと千歳基地RJCJで同じ図**
      (座標表21点が完全一致)なのでRJCC側だけ定義してある。
      弧の中心3つはすべて実測で裏が取れた:
      丘珠ARP 8NM / 函館VOR(HWE) 45NM / 座標表の(6) 45NM・52NM
- **制限空域・警戒区域(ENR 5.1)**: `tools/gen_res.py` → `res.json`(**52区域**)。
  ENR 5.1.1 飛行禁止区域=Nil / 5.1.2 飛行制限区域=RJR1〜3 / 5.1.3 危険区域=Nil /
  5.1.4 空域制限=R-nnn・W-nnn(演習場・射爆撃場・空戦訓練区域)。
  - **RJRは航空法80条**で飛行が制限される区域。RJR1/RJR3は円を経線・緯線で
    半分に切る(「west/north side area of …」)。調整先(Point of Contact)も出す
  - **R-116/R-121 は上限の違う副区画に分かれる**。全体外形1枚で描くと
    R-116北西部(12000ft)まで UNL 扱いになるので、**副区画ごとに1区域として出す**。
    検算: 副区画の面積合計=全体外形(R-116 4204/4206km²、R-121 1774/1774km²)
  - 島嶼部は座標列でなく英文(「A circle radius of 5nm centered at …」
    「beyond 3nm to 5nm radius of …」)。定型文を正規表現で円・円環に起こす
  - 文章でしか書けない2区域だけ `SPEC` に手書き:
    R-130 MISAWA(半径1哩の半円+058°T/108°T の方位線+半径5smの弧)、
    R-521 ROKKASHO(20km/10kmの043°〜133°扇形)
  - **大半が「by NOTAM」「by AIP SUP」運用**。AIPだけでは実施中か判断できないので、
    ポップアップのNOTAM確認の注意書きを消さないこと
  - パースの罠(いずれも実際に踏んだ):
    - 上下限は**罫線と同じ列帯だけ**を見る。行全体を見ると備考欄の
      「TEL 080-…」を下限80ftと誤読する
    - 副区画の `(1)` は**区域名と同じ行**に出る(行末ではなく列37付近。
      右に備考欄が続く)。列位置で見ないと必ず外れる。列を62までに絞るのは
      備考欄の連絡先一覧「(1)DIALECT (2)NYUTA TWR」(列100付近)と区別するため
    - 種別(第3欄)は括弧が3行に折り返す。**列帯を縦に切り出して連結**し、
      各行は空白3つ以上で次の欄に移るのでそこで切る
    - 区域名は「R-/W-数字」か「全部大文字の語の並び」に限定する。
      緩めると "A circle radius of 5nm centered at" 等の文章の続きを区域と誤認する
    - 見出しは目次にも出るので、**本文にしか無い英文見出し**で切り出す
- **原子力施設(飛行回避)**: AIP **ENR 5.3.2.4** が唯一「上空の飛行はできる限り避けること」
  と明記している区域。`tools/gen_nuke.py` → `nuke.json`(**36施設**・多角形座標つき)。
  - **禁止空域ではない**(進入しても違反ではない)。UIでも必ずその旨を出す
  - ★は黄色閃光式灯火(10万cd・40〜60回/分)が設置されている施設(`lit:1`・20施設)
  - 同種の記述を全ENRから探したが他に無い。ENR 5.6(鳥の渡り)は **Nil**
  - pdftotext -layout の段組をパースしている。「(1)」で座標列が始まるので
    その直前の数行をヘッダとして読む。章の前書きが混ざるので「。」を含む行は捨てる

- **DID(人口集中地区)**: 航空法81条・施行規則174条の最低安全高度
  (600m以内の最高障害物+300m)の目安。国土数値情報 **A16-20**(2020年国勢調査)を
  `tools/gen_did.py` → `did.json`(**1049面/8.6万頂点**・1.6MB、gzip後約420KB)。
  - 生データは47都道府県・数百万頂点。**市区町村ごとの区画を全部unionして外周だけ**にし、
    100mで簡略化してこのサイズ。市区町村界はパイロットには不要
  - 障害物と同様、**画面内(外接矩形bで判定)のものだけ描く**。全国1049面を常時描くと重い
  - **interactive:false 必須**。画面全体を覆う面がタップを奪うとWP追加ができなくなる
  - 国土数値情報は利用規約で**出典明示が必要**(レイヤーON時にクレジットへ自動追加)
  - 更新は5年ごと(次は2025年国勢調査の A16-25)。DID自体は法定区域ではない点に注意

- **自衛隊低高度訓練／試験空域**: AIP **ENR 5.2.1**。自衛隊機が最低安全高度以下で
  訓練するために公示されている空域。`tools/gen_tra.py` → `tra.json`(**16区画**)。
  下限は全てSFC、上限は3000〜FL150、使用時間は 2200-1200 DLY(UTC)。
  - **座標だけ本文から自動抽出し、区画の構成(SPEC)は表を pdftoppm で画像化して目視で起こす**。
    1つのAreaが上限高度別に複数区画へ分かれ、しかも高度セルが縦結合されている
    (Area 4の6000は2区画にまたがる)。段組テキストから機械的に読むのは危険
  - `pts` は**リングの配列**(外周+穴)。Area 3-1 は相馬原管制圏を完全に内包するので
    差分が穴になる。**exteriorだけ取ると除外が消える**ので interiors も必ず持つこと
  - 円弧の中心: Nagoya VORTAC / Hamamatsu TACAN / Iwami VOR/DME(ENR 4.1に無いので
    RJOW AD 2.19から取得)。**Area 9-1の点(1)はAIP記載の9NM円に乗っていない(実測7.88NM)**。
    記載半径で描くと段差が出るので、両端の実距離を補間してAIP記載の2点を通す実装にした
  - 進入禁止ではない。UIにその旨を出す

- **民間訓練試験空域**: AIP **ENR 5.3.1**。`tools/gen_civ.py` → `civ.json`(**114区画**)。
  使用にはATMセンターへの訓練計画提出が必要。通過だけなら提出不要だが連絡は要る。
  - 表は `pdftotext -layout` の**列位置**でパースする。列位置はページごとに数文字
    ずれるので、見出し行(`Altitude`/`Facility`)から都度取り直すこと
  - **区画の区切りは番号ではなく「The airspace bounded by …」**。
    番号(13-3等)は座標の途中の行に置かれることがあり、番号で切ると前の区画に
    座標を取り込んでしまう
  - 高度セルは「上限/-----/下限」。**下限はSFCとは限らない**(北海道2-1は8000/4000)
  - 高度・連絡先のセルは複数区画で**縦結合**される。区画内に無ければ、
    高度は最も近いセル・連絡先は直前のセルを引き継ぐ(境界の1〜2区画は誤り得る)
  - 同じ形に高度セルが何段も積まれる区画がある(東北13-3-4〜13-3-14はSFC〜FL140を
    11層)。地図では同形なので下限の最小〜上限の最大にまとめる
  - ⚠ **円弧・除外の指定は形状に反映していない**(59区画に該当)。点を直線で結んだ
    概略形なので、該当区画はポップアップに原文と警告を出す。**消さないこと**
  - 地域コードは表の見出しどおりに訳す。**KK=関東/甲信越**(甲信越だけではない)、
    CK=中部/近畿、CS=中国/四国
  - **関東(KK 4-x/5-x)の連絡先は「New Chitose Information」で正しい**。
    誤りに見えるが、対空援助業務は新千歳空港事務所が遠隔で提供している
    (GEN 3.3の代替機関表でも大島RJTOがNew Chitose Information 134.7MHz)。
    KK 4-x=135.75MHz、KK 5-x=134.8MHz
  - 連絡先セルの区切りは「Controlling / Communication Facility」の見出し。
    行の隙間だけで切ると隣り合う2つの機関(松本Radioと新千歳Information)が混ざる
  - 新幹線・河川・高速道路の中心線で定義された区画は座標が無い。
    **KK4(関東/甲信越 4-1〜4-8)は `tools/gen_kk4.py` で再現済み**(通報で区分名を
    使うため)。他地域の同種の区画は未対応(生成時に件数を出している)

- **KK4の細分**: `tools/gen_kk4.py` → `kk4.json` → gen_civ.py が civ.json に差し込む。
  境界=上越/北陸新幹線・東北新幹線・東北自動車道・利根川・国道125号・
  **埼玉県道38号 加須鴻巣線**(鴻巣で上越新幹線・加須で東北道に接する。
  群馬/栃木県道38号足利千代田線は緯度が北すぎて不適)。
  - 4-4〜4-8は線を平面グラフにして polygonize。**区分と面の対応は全体最適で決める**
    (順に貪欲だと4-5が4-6の面を先に取る)
  - 4-2/4-3はpolygonizeでは閉じない(利根川が横切る)ので、境界を明示的に
    組み立ててから県道38号で分割する
  - **4-4も明示的に組み立てる**。南辺(線B)を西へ約480m伸ばして東北新幹線に当て、
    利根川まで回す。東北新幹線と東北自動車道の交点(久喜付近)より南では
    高速道路が区域の**内側**を通るため、polygonizeだと帯が切れて欠ける。
    SWIMの空域プロファイルサービスの表示でも南辺は西に伸びている(ユーザー確認済み)
  - **OSMの線は上下線・上下車線が別way**で、駅や橋でも切れている。最長成分を
    そのまま使うと端が数km足りない(4-1の北西端)。両端に近い成分を選び、
    足りない分は隣の断片を継ぎ足す(`near()`)
  - 県道38号はOSMの経路が東北道まで1.6km・上越新幹線まで0.6km届かないので直線で延長。
    そのぶん加須の西に約10km²の隙間が残るが、**SWIMの表示ではそこは4-2側**で
    境界は4-5と同じ線(東北自動車道)なので、区分間の隙間は4-2に取り込む
    (`GAP_TO`。ユーザーがSWIMで確認)
  - ⚠ 形状はOSM由来の**目安**。ポップアップにその旨を出す(`osm:1`)。**消さないこと**

- **ヘリポート・滑空場**: `tools/gen_hp.py` → `hp.json`(**59件**)。
  ICAOの位置指示記号を持たないので ad.json には入らないもの。
  - **AD 1.4.2 地点略号の無いヘリポート** 25件 / **AD 1.4.1 空港等** 4件 /
    **ENR 5.5.1 GLIDER ACTIVITIES 滑空場** 31件
  - 緯度と経度が**別の行**に来る。間隔は項目により異なり、
    **ホンダエアポートは7行離れている**ので窓を9行にしてある(狭いと落ちる)
  - AD 1.4 の表は**縦組みを寝かせた段組**で名称が座標の周囲に散らばる。
    座標の**列位置より左**だけを見て、かつ**前の項目の座標行より手前は見ない**
    ことで隣の名前の混入を抑えている。それでも取り違えは残るので、
    ポップアップに「名称は取り違えていることがある(座標は信頼可)」と出す
  - 滑空場は関東に集中している(妻沼・板倉・読売加須・大利根・羽生・真壁など)。
    グライダー活動の要注意エリアとして実用的

### ログの連絡先の自動記入(2026-07-30)
- `airspaceAt(w)` が地点を含む空域を返す。優先順は **管制圏→情報圏→特別管制区→
  民間訓練試験空域→自衛隊低高度訓練空域**(上ほど連絡の義務が強い)
- 判定は `inArea()`。tra.jsonの `pts` は**外周+穴のリング配列**なので、
  1本目に入っていて2本目以降(穴)に入っていないことを見る
- 訓練空域はレイヤーを開いたときだけ判定できる(遅延読込。`CIV`/`TRA` がnullなら飛ばす)
- ASP_POLYの `rmk` は「Tokyo Tower 118.1/124.35」形式。`splitCall()` で
  呼出名と周波数に割る。周波数が無い注記(運用時間等)は呼出名側に残る

### 実装メモ
- 幾何計算はshapely(局所equirect平面、NM単位)。円=buffer、半平面クリップ、
  円同士の交点(立川/入間)、DMEアーク(成田PCAの9.4/5.4NM)対応済み
- 横田CTR=5nm円−立川CTR−入間CTR、木更津CTRは南側条件の入れ子(北帯1000/中帯1500/南2000)
- 東京PCA=中央円+NE扇形6帯+南アーム6帯+NEXUS+NR2(北西)4帯の計18区画。
  NR2は0600-1000UTCのみ。詳細は gen_asp.py のコメント参照

## 9. バックログ

**BACKLOG.md に分離した。** 着手時はセッション冒頭で「BACKLOG.mdの◯番」と指示を受ける。

## 10. UIデザイン規約

- 配色はCSS変数のみ使用: `--grn`(正常値/緑) `--amb`(燃料・注意/アンバー)
  `--mag`(フライトプラン/マゼンタ) `--cyn`(Direct-To・LIVE/シアン) `--red`(警告) `--blu`(管制圏・共有系)
- 数値・方位・時刻は必ず `--mono`(IBM Plex Mono)、方位は3桁+°、時間はh:mm
- GPS由来の値には「LIVE」タグ、デモデータには「DEMO」タグを必ず表示(実データとの混同防止)
- モバイル前提: タップ領域を小さくしない、テーブルは横スクロール許容

## 11. 既知の制限・注意

- 飛行ログのOFF/ONは手動ボタン(自動検出なし)。離陸ボタン押し忘れ対策は未実装
- ETE合計は各レグの風補正GSの積算。実GS連動時は現在GSを全レグに適用する簡易方式
- **関東の空域ポリゴンはAIP 2026-07-09現在の正式形状**(凡例に基準日を表示)。
  **関東以外の空域円は開発用サンプルのまま**(「※概略円」表記を消さないこと)。
  飛行場ポップアップの標高・滑走路は関東の一部を除きサンプル値
- localStorage依存のため、ブラウザのサイトデータ削除でログが消える(README記載済み、CSV書出で退避)
- 共有コードに位置情報(ルート)が含まれる。取り扱い注意の旨はユーザー了承済み

## 12. ユーザーコンテキスト

- 職業ヘリパイロット(日本)。航空用語はプロレベル、遠慮なく専門用語でよい
- 開発の進め方: 機能要望が来る→動くバージョンを丸ごと1ファイルで返す、の反復
- 風向の真/磁の区別、WCA、Direct-To等の概念説明は不要
- 安全に関わる注記(参考情報である旨)はユーザーも了承の上で維持している
